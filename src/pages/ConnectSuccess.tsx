const ConnectSuccess = () => {
  return (
    <div style={{ padding: "2rem", fontSize: "1.2rem" }}>
      ✅ Google Calendar connected! You can now use the bot.
    </div>
  );
};

export default ConnectSuccess;
